name: consu

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  noop:
    name: Noop
    runs-on: ubuntu-latest
    steps:
    - name: Noop
      run: echo "Nothing to do"
